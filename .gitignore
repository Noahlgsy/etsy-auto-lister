# ── Secrets & environment ──────────────────────────────
.env
.env.*
!.env.example

# ── Python ─────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# ── OS / editors ───────────────────────────────────────
.DS_Store
.idea/
.vscode/

# ── Generated product output (keep the example) ────────
products/*
!products/exemple-produit/

# ── Local run config (keep the .example templates) ─────
batch.txt

# ── Caches & large regenerable / scraped data ──────────
taxonomy_cache.json
data/competitors/
data/easypic/

# ── Base locale ventes/finances (données perso, jamais versionnées) ──
data/finance.db
data/finance.db-wal
data/finance.db-shm
data/listing_thumbs/

# ── Claude Code local ───────────────────────────────────
.claude/settings.local.json
